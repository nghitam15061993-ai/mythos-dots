# Contract đã deploy
NEXT_PUBLIC_CONTRACT_ADDRESS=0xYourDeployedContract

# Chain: "sepolia" hoặc "mainnet" (trả bằng ETH native, không cần USDC)
NEXT_PUBLIC_CHAIN=sepolia

# URL agent service (quiz + ký pass)
NEXT_PUBLIC_AGENT_URL=http://localhost:8787

# WalletConnect projectId — https://cloud.reown.com (public, an toàn để lộ)
NEXT_PUBLIC_WC_PROJECT_ID=6661e3693eaf2c1128ed4d9fae6c011d
